/** Dark rail width (px) — keep `Sidebar`, `AppShell`, `AppTopBar`, and fixed overlays aligned. */
export const SIDEBAR_WIDTH_PX = 280;

/** Full class strings so Tailwind can detect arbitrary values. */
export const sidebarWidthClass = "md:w-[280px]";
export const mainOffsetFromSidebarClass = "md:pl-[280px]";
export const topBarLeftOffsetClass = "md:left-[280px]";
