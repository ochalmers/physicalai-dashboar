node_modules
dist
*.local
.DS_Store

# Editor / agent metadata
.cursor/

# Design inspo screenshots (too large for repo history)
inspo/

# Large export archives (keep generated artifacts out of git)
assets/*.zip
assets/3d/**/*.zip
